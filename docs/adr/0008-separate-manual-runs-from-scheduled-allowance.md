# Reserve the daily allowance for scheduled implementation

AutoProgress records an explicit `manual` or `scheduled` trigger source and claims the daily allowance only for scheduled `implement-batch`. Human calls remain unlimited by date while still obeying workspace, recovery, review, and safety gates; this preserves automation resource bounds without turning a scheduling policy into a human administration limit.
