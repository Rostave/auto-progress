# 将运行状态保存在受管项目之外

AutoProgress 将阶段检查点与恢复义务保存在 Codex 本地状态根目录，并按项目和运行隔离，而不写入受管项目工作树、`.git` common directory 或其他托管工具内部目录。项目内或版本控制内部存储更容易就近定位和跨 worktree 共享，但会污染工作区、依赖特定版本控制实现并可能被其清理流程删除；仓库外状态需要稳定项目身份与独立生命周期管理，却能支持 Git 之外的托管工具并让模型不承担状态机。
