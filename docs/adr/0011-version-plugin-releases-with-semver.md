# Version plugin releases with SemVer 2.0

AutoProgress plugin releases use `MAJOR.MINOR.PATCH+codex.YYYYMMDDHHMMSS`: numeric SemVer components communicate compatibility while the UTC build metadata distinguishes Codex cache artifacts without changing precedence. Plugin release versions remain independent from project-policy schema versions, and releases must document deterministic configuration migration plus gradual compatibility for legacy improvement filenames.
