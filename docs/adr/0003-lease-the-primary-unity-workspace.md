# 租用原始 Unity 工作区进行每日维护

每日维护在人工许可后临时独占原始 Unity 项目目录，而不是创建第二个 Unity worktree。独立 worktree 隔离性更强，但会生成另一份体积很大的 `Library`，并迫使现有 Unity MCP 与其他工作流适配新路径；租约模型复用现有项目配置和缓存，代价是运行期间禁止人工及其他工作流并发使用，并要求任何脏状态都直接跳过而不能自动 stash、清理或覆盖。
